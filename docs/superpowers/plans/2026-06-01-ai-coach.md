# AI Coach — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add AI Coach to Reharm Studio — a "Chamar IA" button in the Navbar that opens a guided wizard (zero state) or a context-aware side panel (mid-session), delivers chord progression suggestions, and shows 🟢🟡🔴 badges per chord in the HarmonicField.

**Architecture:** New `AIContext` holds session snapshot + AI status. Two new Vercel Edge endpoints (`ai-suggest`, `ai-badges`) call Groq. `AIWizard` (modal) and `AIPanel` (sliding panel) are controlled by `AIContext`. `HarmonicField` receives badges as props — no internal AI logic.

**Tech Stack:** React 18 + TypeScript, Vercel Edge Functions, Groq `llama-3.3-70b-versatile`, Tailwind CSS, Vitest (added in Task 1)

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `src/types/index.ts` | Modify | Add `AISession`, `AIStatus`, `AISuggestion`, `ChordBadgeData`, `BadgeLevel` |
| `src/contexts/AIContext.tsx` | Create | Session snapshot, AI status, suggestion, badges, open/close wizard/panel |
| `src/components/AIButton.tsx` | Create | Navbar button with 4 visual states |
| `src/components/AIWizard.tsx` | Create | Modal 4-step wizard (Música → Estilo → BPM → Feeling → Generate) |
| `src/components/AIPanel.tsx` | Create | Sliding panel for mid-session context + suggestion display |
| `src/components/ChordBadge.tsx` | Create | Badge 🟢🟡🔴 with click-to-open Groq explanation popover |
| `api/ai-suggest.ts` | Create | Edge function: session snapshot + optional intention → `{ chords, explanation }` |
| `api/ai-badges.ts` | Create | Edge function: list of chords + context → `{ badges: ChordBadgeData[] }` |
| `src/components/Navbar.tsx` | Modify | Add `<AIButton>` between LangDropdown and theme toggle |
| `src/components/HarmonicField.tsx` | Modify | Accept `badges` prop, render `<ChordBadge>` over each chord button |
| `src/App.tsx` | Modify | Wrap with `<AIProvider>`, sync session on state change, handle `acceptSuggestion` |
| `src/i18n/pt-BR.ts` | Modify | Add `ai.*` keys |
| `src/i18n/en.ts` | Modify | Add `ai.*` keys |
| `src/i18n/es.ts` | Modify | Add `ai.*` keys |
| `src/lib/ai-utils.ts` | Create | Pure helpers: `parseGroqBadges()`, `sessionIsEmpty()` — testable without DOM |
| `src/lib/ai-utils.test.ts` | Create | Vitest unit tests for pure helpers |

---

## Task 1: Setup Vitest + Types

**Files:**
- Modify: `package.json`
- Modify: `vite.config.ts`
- Modify: `src/types/index.ts`
- Create: `src/lib/ai-utils.ts`
- Create: `src/lib/ai-utils.test.ts`

- [ ] **Step 1: Instalar Vitest**

```bash
cd "/Volumes/SSD Interno/Projetos ClaudeCode/Reharm"
npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom
```

- [ ] **Step 2: Configurar Vitest no vite.config.ts**

```ts
// vite.config.ts — substituir o conteúdo
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: [],
  },
})
```

- [ ] **Step 3: Adicionar script de test ao package.json**

No bloco `"scripts"`, adicionar:
```json
"test": "vitest run",
"test:ui": "vitest --ui"
```

- [ ] **Step 4: Adicionar tipos AI ao src/types/index.ts**

Acrescentar ao final do arquivo:

```ts
// --- AI Coach ---

export interface AISession {
  song: { artist: string; title: string } | null
  style: string
  bpm: number
  feeling: string[]
  chords: string[]
  tonicNum: number | null
}

export type AIStatus = 'idle' | 'loading' | 'active' | 'dismissed'

export interface AISuggestion {
  chords: string[]
  explanation: string
}

export type BadgeLevel = 'good' | 'ok' | 'bad'

export interface ChordBadgeData {
  chord: string
  level: BadgeLevel
  explanation: string
}

export interface AIContextValue {
  status: AIStatus
  session: AISession
  suggestion: AISuggestion | null
  badges: Record<string, ChordBadgeData>
  wizardOpen: boolean
  panelOpen: boolean
  updateSession: (patch: Partial<AISession>) => void
  callAI: () => void
  acceptSuggestion: (onApply: (chords: string[]) => void) => void
  dismissAI: () => void
}
```

- [ ] **Step 5: Escrever o failing test para ai-utils.ts**

Criar `src/lib/ai-utils.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { sessionIsEmpty, parseGroqBadges } from './ai-utils'
import type { AISession, ChordBadgeData } from '../types'

const emptySession: AISession = {
  song: null,
  style: 'House',
  bpm: 124,
  feeling: [],
  chords: [],
  tonicNum: null,
}

describe('sessionIsEmpty', () => {
  it('returns true when session has no song and no chords', () => {
    expect(sessionIsEmpty(emptySession)).toBe(true)
  })

  it('returns false when session has a song', () => {
    expect(sessionIsEmpty({ ...emptySession, song: { artist: 'Lionel Richie', title: 'Stuck on You' } })).toBe(false)
  })

  it('returns false when session has chords', () => {
    expect(sessionIsEmpty({ ...emptySession, chords: ['Fmaj7', 'Am7'] })).toBe(false)
  })
})

describe('parseGroqBadges', () => {
  it('maps groq badge array to Record keyed by chord label', () => {
    const raw: ChordBadgeData[] = [
      { chord: 'Fmaj9', level: 'good', explanation: 'Tônico perfeito' },
      { chord: 'Am11', level: 'ok', explanation: 'Funciona com cuidado' },
    ]
    const result = parseGroqBadges(raw)
    expect(result['Fmaj9'].level).toBe('good')
    expect(result['Am11'].explanation).toBe('Funciona com cuidado')
  })

  it('returns empty record for empty array', () => {
    expect(parseGroqBadges([])).toEqual({})
  })
})
```

- [ ] **Step 6: Rodar o test — deve falhar**

```bash
cd "/Volumes/SSD Interno/Projetos ClaudeCode/Reharm"
npm test
```

Esperado: FAIL — `ai-utils` module not found.

- [ ] **Step 7: Criar src/lib/ai-utils.ts com implementação mínima**

```ts
import type { AISession, ChordBadgeData, BadgeLevel } from '../types'

export function sessionIsEmpty(session: AISession): boolean {
  return session.song === null && session.chords.length === 0
}

export function parseGroqBadges(raw: ChordBadgeData[]): Record<string, ChordBadgeData> {
  return raw.reduce((acc, b) => ({ ...acc, [b.chord]: b }), {} as Record<string, ChordBadgeData>)
}
```

- [ ] **Step 8: Rodar o test — deve passar**

```bash
npm test
```

Esperado: PASS (2 suites, 5 tests).

- [ ] **Step 9: Commit**

```bash
git add package.json vite.config.ts src/types/index.ts src/lib/ai-utils.ts src/lib/ai-utils.test.ts
git commit -m "feat: add Vitest, AI types, and ai-utils helpers"
```

---

## Task 2: AIContext

**Files:**
- Create: `src/contexts/AIContext.tsx`

- [ ] **Step 1: Criar src/contexts/AIContext.tsx**

```tsx
import { createContext, useContext, useState, useCallback } from 'react'
import type { AIStatus, AISession, AISuggestion, ChordBadgeData, AIContextValue } from '../types'
import { parseGroqBadges } from '../lib/ai-utils'

const defaultSession: AISession = {
  song: null,
  style: 'House',
  bpm: 124,
  feeling: [],
  chords: [],
  tonicNum: null,
}

const AIContext = createContext<AIContextValue>({
  status: 'idle',
  session: defaultSession,
  suggestion: null,
  badges: {},
  wizardOpen: false,
  panelOpen: false,
  updateSession: () => {},
  callAI: () => {},
  acceptSuggestion: () => {},
  dismissAI: () => {},
})

export function useAI() {
  return useContext(AIContext)
}

export function AIProvider({ children }: { children: React.ReactNode }) {
  const [status, setStatus] = useState<AIStatus>('idle')
  const [session, setSession] = useState<AISession>(defaultSession)
  const [suggestion, setSuggestion] = useState<AISuggestion | null>(null)
  const [badges, setBadges] = useState<Record<string, ChordBadgeData>>({})
  const [wizardOpen, setWizardOpen] = useState(false)
  const [panelOpen, setPanelOpen] = useState(false)

  const updateSession = useCallback((patch: Partial<AISession>) => {
    setSession(prev => ({ ...prev, ...patch }))
  }, [])

  // callAI: opens wizard if session is empty, panel otherwise
  // The component calling this passes the current session via updateSession first
  const callAI = useCallback(() => {
    const isEmpty = session.song === null && session.chords.length === 0
    if (isEmpty) {
      setWizardOpen(true)
    } else {
      setPanelOpen(true)
    }
  }, [session])

  const acceptSuggestion = useCallback((onApply: (chords: string[]) => void) => {
    if (!suggestion) return
    onApply(suggestion.chords)
    setSuggestion(null)
    setPanelOpen(false)
    setWizardOpen(false)
    setStatus('active')
  }, [suggestion])

  const dismissAI = useCallback(() => {
    setSuggestion(null)
    setBadges({})
    setStatus('dismissed')
    setWizardOpen(false)
    setPanelOpen(false)
  }, [])

  // Called by AIWizard and AIPanel after Groq returns
  const setSuggestionFromGroq = useCallback((raw: AISuggestion) => {
    setSuggestion(raw)
    setStatus('active')
  }, [])

  // Called after acceptSuggestion to fetch badges for accepted chords
  const fetchBadges = useCallback(async (chords: string[], ctx: Omit<AISession, 'chords'>) => {
    try {
      const res = await fetch('/api/ai-badges', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ chords, style: ctx.style, bpm: ctx.bpm, feeling: ctx.feeling, tonicNum: ctx.tonicNum }),
      })
      const data = await res.json() as { badges: ChordBadgeData[] }
      setBadges(parseGroqBadges(data.badges ?? []))
    } catch {
      // badges are non-critical, fail silently
    }
  }, [])

  return (
    <AIContext.Provider value={{
      status,
      session,
      suggestion,
      badges,
      wizardOpen,
      panelOpen,
      updateSession,
      callAI,
      acceptSuggestion,
      dismissAI,
    }}>
      {/* Expose internal setters for AIWizard and AIPanel via a separate context */}
      <AIInternalContext.Provider value={{ setStatus, setSuggestionFromGroq, fetchBadges, setWizardOpen, setPanelOpen }}>
        {children}
      </AIInternalContext.Provider>
    </AIContext.Provider>
  )
}

// Internal context: only AIWizard and AIPanel use this
interface AIInternalValue {
  setStatus: (s: AIStatus) => void
  setSuggestionFromGroq: (s: AISuggestion) => void
  fetchBadges: (chords: string[], ctx: Omit<AISession, 'chords'>) => Promise<void>
  setWizardOpen: (v: boolean) => void
  setPanelOpen: (v: boolean) => void
}

const AIInternalContext = createContext<AIInternalValue>({
  setStatus: () => {},
  setSuggestionFromGroq: () => {},
  fetchBadges: async () => {},
  setWizardOpen: () => {},
  setPanelOpen: () => {},
})

export function useAIInternal() {
  return useContext(AIInternalContext)
}
```

- [ ] **Step 2: Verificar que TypeScript compila**

```bash
cd "/Volumes/SSD Interno/Projetos ClaudeCode/Reharm"
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/contexts/AIContext.tsx
git commit -m "feat: add AIContext with session snapshot and AI status management"
```

---

## Task 3: Endpoints Groq — ai-suggest e ai-badges

**Files:**
- Create: `api/ai-suggest.ts`
- Create: `api/ai-badges.ts`

- [ ] **Step 1: Criar api/ai-suggest.ts**

```ts
// POST /api/ai-suggest
// Body: { session: AISession, intention?: string, lang?: string }
// Returns: { chords: string[], explanation: string }

export const config = { runtime: 'edge' }

const LANG_INSTRUCTION: Record<string, string> = {
  en: 'All text values must be in English.',
  es: 'Todos los valores de texto deben estar en español.',
  'pt-BR': 'Todos os valores de texto devem estar em português do Brasil.',
}

export default async function handler(req: Request) {
  if (req.method === 'OPTIONS') {
    return new Response(null, {
      headers: {
        'Access-Control-Allow-Origin': '*',
        'Access-Control-Allow-Methods': 'POST, OPTIONS',
        'Access-Control-Allow-Headers': 'Content-Type',
      },
    })
  }

  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method not allowed' }), { status: 405 })
  }

  try {
    const body = await req.json() as {
      session: {
        song: { artist: string; title: string } | null
        style: string
        bpm: number
        feeling: string[]
        chords: string[]
        tonicNum: number | null
      }
      intention?: string
      lang?: string
    }

    const { session, intention, lang = 'pt-BR' } = body
    const langInstruction = LANG_INSTRUCTION[lang] ?? LANG_INSTRUCTION['pt-BR']
    const groqKey = process.env.GROQ_API_KEY
    if (!groqKey) throw new Error('GROQ_API_KEY não configurada')

    const songContext = session.song
      ? `The reference song is "${session.song.title}" by ${session.song.artist}.`
      : 'No reference song provided.'

    const chordsContext = session.chords.length > 0
      ? `The user already has these chords: ${session.chords.join(' – ')}.`
      : 'The user has no chords yet.'

    const intentionContext = intention
      ? `The user's intention: "${intention}".`
      : ''

    const prompt = `You are an expert music producer and harmony coach specializing in electronic dance music.

${songContext}
Target style: ${session.style}
Target BPM: ${session.bpm}
Desired feeling: ${session.feeling.join(', ') || 'not specified'}
${chordsContext}
${intentionContext}

Suggest a 4-chord progression that fits this context perfectly.

IMPORTANT: ${langInstruction}
Respond ONLY with valid JSON, no markdown, no extra text:
{
  "chords": ["Fmaj9", "Am11", "Bbmaj9", "C9sus4"],
  "explanation": "Two sentences explaining why this progression works for this style and feeling."
}`

    const res = await fetch('https://api.groq.com/openai/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${groqKey}`,
      },
      body: JSON.stringify({
        model: 'llama-3.3-70b-versatile',
        messages: [{ role: 'user', content: prompt }],
        temperature: 0.5,
        response_format: { type: 'json_object' },
      }),
    })

    if (!res.ok) {
      const err = await res.text()
      throw new Error(`Groq error: ${res.status} — ${err}`)
    }

    const data = await res.json() as { choices: { message: { content: string } }[] }
    const parsed = JSON.parse(data.choices?.[0]?.message?.content ?? '{}') as {
      chords?: string[]
      explanation?: string
    }

    return new Response(
      JSON.stringify({
        chords: parsed.chords ?? [],
        explanation: parsed.explanation ?? '',
      }),
      { headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } },
    )
  } catch (err) {
    return new Response(
      JSON.stringify({ error: 'Erro ao gerar sugestão', detail: String(err) }),
      { status: 500, headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } },
    )
  }
}
```

- [ ] **Step 2: Criar api/ai-badges.ts**

```ts
// POST /api/ai-badges
// Body: { chords: string[], style: string, bpm: number, feeling: string[], tonicNum: number | null, lang?: string }
// Returns: { badges: ChordBadgeData[] }

export const config = { runtime: 'edge' }

const LANG_INSTRUCTION: Record<string, string> = {
  en: 'All text values must be in English.',
  es: 'Todos los valores de texto deben estar en español.',
  'pt-BR': 'Todos os valores de texto devem estar em português do Brasil.',
}

const NOTE_NAMES = ['C', 'C#', 'D', 'Eb', 'E', 'F', 'F#', 'G', 'Ab', 'A', 'Bb', 'B']

export default async function handler(req: Request) {
  if (req.method === 'OPTIONS') {
    return new Response(null, {
      headers: {
        'Access-Control-Allow-Origin': '*',
        'Access-Control-Allow-Methods': 'POST, OPTIONS',
        'Access-Control-Allow-Headers': 'Content-Type',
      },
    })
  }

  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method not allowed' }), { status: 405 })
  }

  try {
    const body = await req.json() as {
      chords: string[]
      style: string
      bpm: number
      feeling: string[]
      tonicNum: number | null
      lang?: string
    }

    const { chords, style, bpm, feeling, tonicNum, lang = 'pt-BR' } = body
    const langInstruction = LANG_INSTRUCTION[lang] ?? LANG_INSTRUCTION['pt-BR']
    const groqKey = process.env.GROQ_API_KEY
    if (!groqKey) throw new Error('GROQ_API_KEY não configurada')

    const tonicLabel = tonicNum !== null ? NOTE_NAMES[tonicNum] : 'unspecified'

    const prompt = `You are a music harmony coach. Evaluate each chord in context.

Context:
- Key: ${tonicLabel}
- Style: ${style}
- BPM: ${bpm}
- Feeling: ${feeling.join(', ') || 'not specified'}

Chords to evaluate: ${chords.join(', ')}

For each chord, give:
- level: "good" (fits perfectly), "ok" (works with a caveat), or "bad" (clashes with the context)
- explanation: exactly 2 sentences in the target language explaining why

IMPORTANT: ${langInstruction}
Respond ONLY with valid JSON, no markdown:
{
  "badges": [
    { "chord": "Fmaj9", "level": "good", "explanation": "..." },
    { "chord": "Am11", "level": "ok", "explanation": "..." }
  ]
}`

    const res = await fetch('https://api.groq.com/openai/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        Authorization: `Bearer ${groqKey}`,
      },
      body: JSON.stringify({
        model: 'llama-3.3-70b-versatile',
        messages: [{ role: 'user', content: prompt }],
        temperature: 0.2,
        response_format: { type: 'json_object' },
      }),
    })

    if (!res.ok) {
      const err = await res.text()
      throw new Error(`Groq error: ${res.status} — ${err}`)
    }

    const data = await res.json() as { choices: { message: { content: string } }[] }
    const parsed = JSON.parse(data.choices?.[0]?.message?.content ?? '{}') as {
      badges?: { chord: string; level: string; explanation: string }[]
    }

    return new Response(
      JSON.stringify({ badges: parsed.badges ?? [] }),
      { headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } },
    )
  } catch (err) {
    return new Response(
      JSON.stringify({ error: 'Erro ao avaliar acordes', detail: String(err) }),
      { status: 500, headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } },
    )
  }
}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add api/ai-suggest.ts api/ai-badges.ts
git commit -m "feat: add ai-suggest and ai-badges edge functions"
```

---

## Task 4: AIButton

**Files:**
- Create: `src/components/AIButton.tsx`

- [ ] **Step 1: Criar src/components/AIButton.tsx**

```tsx
import { useAI } from '../contexts/AIContext'
import { useTranslation } from 'react-i18next'

export function AIButton() {
  const { t } = useTranslation()
  const { status, callAI } = useAI()

  const isLoading = status === 'loading'
  const isActive = status === 'active'

  return (
    <button
      onClick={callAI}
      disabled={isLoading}
      className="btn-neumorphic px-3 py-1.5 text-xs font-mono rounded-xl flex items-center gap-1.5 transition-all"
      style={{
        color: isActive ? 'var(--color-primary)' : 'var(--color-muted)',
        borderColor: isActive ? 'var(--color-primary)' : undefined,
        opacity: isLoading ? 0.7 : 1,
      }}
      title={isActive ? t('ai.active') : t('ai.callButton')}
    >
      {isLoading ? (
        <span className="inline-block w-3 h-3 border border-current border-t-transparent rounded-full animate-spin" />
      ) : (
        <span
          className={isActive ? 'animate-pulse' : ''}
          style={{ fontSize: '12px', lineHeight: 1 }}
        >
          ✦
        </span>
      )}
      <span className="hidden sm:inline">
        {isLoading ? t('ai.thinking') : isActive ? t('ai.active') : t('ai.callButton')}
      </span>
    </button>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/AIButton.tsx
git commit -m "feat: add AIButton component with 4 visual states"
```

---

## Task 5: i18n — novas chaves AI

**Files:**
- Modify: `src/i18n/pt-BR.ts`
- Modify: `src/i18n/en.ts`
- Modify: `src/i18n/es.ts`

- [ ] **Step 1: Adicionar chaves ao pt-BR.ts**

Adicionar ao final do objeto de tradução (antes do fechamento `}`):

```ts
  ai: {
    callButton: 'Chamar IA',
    thinking: 'Pensando...',
    active: 'IA ativa',
    dismiss: 'Dispensar IA',
    wizardTitle: 'Vamos criar juntos',
    step1: 'Qual música você quer remixar?',
    step1Skip: 'Pular',
    step2: 'Que estilo você está fazendo?',
    step3: 'Que BPM você está buscando?',
    step3Suggestion: 'Sugestão:',
    step4: 'Qual o feeling que você quer?',
    generate: 'Gerar',
    generating: 'Gerando sua progressão...',
    back: '← Voltar',
    next: 'Próximo →',
    suggestion: 'Sugestão da IA',
    accept: 'Aceitar',
    edit: 'Editar',
    discard: 'Descartar',
    badgeGood: 'Funciona perfeitamente',
    badgeOk: 'Funciona com ressalva',
    badgeBad: 'Foge do contexto',
    intentionQuestion: 'O que você prefere?',
    intentionResolve: 'Resolver a tensão',
    intentionIntensify: 'Manter e intensificar',
    intentionSurprise: 'Me surpreenda',
    panelContext: 'IA ativa — lendo seu contexto...',
  },
```

- [ ] **Step 2: Adicionar chaves ao en.ts**

```ts
  ai: {
    callButton: 'Call AI',
    thinking: 'Thinking...',
    active: 'AI active',
    dismiss: 'Dismiss AI',
    wizardTitle: "Let's create together",
    step1: 'Which song do you want to remix?',
    step1Skip: 'Skip',
    step2: 'Which style are you going for?',
    step3: 'What BPM are you targeting?',
    step3Suggestion: 'Suggestion:',
    step4: 'What feeling do you want?',
    generate: 'Generate',
    generating: 'Generating your progression...',
    back: '← Back',
    next: 'Next →',
    suggestion: 'AI Suggestion',
    accept: 'Accept',
    edit: 'Edit',
    discard: 'Discard',
    badgeGood: 'Works perfectly',
    badgeOk: 'Works with a caveat',
    badgeBad: 'Clashes with the context',
    intentionQuestion: 'What do you prefer?',
    intentionResolve: 'Resolve the tension',
    intentionIntensify: 'Keep and intensify',
    intentionSurprise: 'Surprise me',
    panelContext: 'AI active — reading your context...',
  },
```

- [ ] **Step 3: Adicionar chaves ao es.ts**

```ts
  ai: {
    callButton: 'Llamar IA',
    thinking: 'Pensando...',
    active: 'IA activa',
    dismiss: 'Descartar IA',
    wizardTitle: 'Vamos crear juntos',
    step1: '¿Qué canción quieres remezclar?',
    step1Skip: 'Saltar',
    step2: '¿Qué estilo estás haciendo?',
    step3: '¿Qué BPM buscas?',
    step3Suggestion: 'Sugerencia:',
    step4: '¿Qué feeling quieres?',
    generate: 'Generar',
    generating: 'Generando tu progresión...',
    back: '← Volver',
    next: 'Siguiente →',
    suggestion: 'Sugerencia de la IA',
    accept: 'Aceptar',
    edit: 'Editar',
    discard: 'Descartar',
    badgeGood: 'Funciona perfectamente',
    badgeOk: 'Funciona con reservas',
    badgeBad: 'Choca con el contexto',
    intentionQuestion: '¿Qué prefieres?',
    intentionResolve: 'Resolver la tensión',
    intentionIntensify: 'Mantener e intensificar',
    intentionSurprise: 'Sorpréndeme',
    panelContext: 'IA activa — leyendo tu contexto...',
  },
```

- [ ] **Step 4: Commit**

```bash
git add src/i18n/pt-BR.ts src/i18n/en.ts src/i18n/es.ts
git commit -m "feat: add ai.* i18n keys in PT/EN/ES"
```

---

## Task 6: Navbar — integrar AIButton

**Files:**
- Modify: `src/components/Navbar.tsx`

- [ ] **Step 1: Adicionar import do AIButton ao Navbar.tsx**

Acrescentar a linha após os imports existentes:
```tsx
import { AIButton } from './AIButton'
```

- [ ] **Step 2: Inserir `<AIButton>` no bloco de ações do Navbar**

Localizar o bloco `{/* Ações */}` e inserir `<AIButton />` como primeiro filho, antes do `<LangDropdown />`:

```tsx
      {/* Ações */}
      <div className="flex items-center gap-2">
        <AIButton />
        <LangDropdown />
        {/* resto inalterado */}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Rodar o dev server e verificar que o botão aparece**

```bash
npm run dev
```

Abrir `http://localhost:5173`. O botão `✦ Chamar IA` deve aparecer no Navbar, à esquerda do seletor de idioma, com cor muted. Clicar nele não deve abrir nada ainda (AIWizard ainda não existe).

- [ ] **Step 5: Commit**

```bash
git add src/components/Navbar.tsx
git commit -m "feat: add AIButton to Navbar"
```

---

## Task 7: AIWizard

**Files:**
- Create: `src/components/AIWizard.tsx`

- [ ] **Step 1: Criar src/components/AIWizard.tsx**

```tsx
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { useAI, useAIInternal } from '../contexts/AIContext'
import { SongSearch, type SongAnalysis } from './SongSearch'

const STYLES = ['House', 'Deep House', 'Gospel House', 'Afro House', 'Tech House', 'Jazz', 'Lo-fi', 'Techno']

const FEELINGS = [
  { id: 'groovy', label: 'Groovy' },
  { id: 'soulful', label: 'Soulful' },
  { id: 'dark', label: 'Dark' },
  { id: 'tribal', label: 'Tribal' },
  { id: 'jazzy', label: 'Jazzy' },
  { id: 'uplifting', label: 'Uplifting' },
  { id: 'melancólico', label: 'Melancólico' },
  { id: 'energético', label: 'Energético' },
]

export function AIWizard() {
  const { t, i18n } = useTranslation()
  const { wizardOpen, session } = useAI()
  const { setWizardOpen, setSuggestionFromGroq, setStatus, fetchBadges } = useAIInternal()
  const { updateSession } = useAI()

  const [step, setStep] = useState(0)
  const [localSong, setLocalSong] = useState<SongAnalysis | null>(null)
  const [localStyle, setLocalStyle] = useState(session.style)
  const [localBpm, setLocalBpm] = useState(session.bpm)
  const [localFeeling, setLocalFeeling] = useState<string[]>([])
  const [generating, setGenerating] = useState(false)
  const [bpmSuggestion, setBpmSuggestion] = useState<number | null>(null)

  if (!wizardOpen) return null

  const toggleFeeling = (id: string) =>
    setLocalFeeling(prev =>
      prev.includes(id)
        ? prev.filter(f => f !== id)
        : prev.length < 2 ? [...prev, id] : prev
    )

  const handleSongSelect = (song: SongAnalysis) => {
    setLocalSong(song)
    if (song.bpm_original) setBpmSuggestion(Math.round(song.bpm_original * 1.5))
    setStep(1)
  }

  const handleGenerate = async () => {
    setGenerating(true)
    setStatus('loading')

    const snap = {
      song: localSong ? { artist: localSong.artist, title: localSong.title } : null,
      style: localStyle,
      bpm: localBpm,
      feeling: localFeeling,
      chords: [],
      tonicNum: null,
    }

    updateSession(snap)

    try {
      const res = await fetch('/api/ai-suggest', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ session: snap, lang: i18n.language }),
      })
      const data = await res.json() as { chords?: string[]; explanation?: string }
      setSuggestionFromGroq({ chords: data.chords ?? [], explanation: data.explanation ?? '' })
      setGenerating(false)
      setStep(4)
    } catch {
      setStatus('idle')
      setGenerating(false)
    }
  }

  const dots = [0, 1, 2, 3]

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center"
      style={{ background: 'rgba(0,0,0,0.6)' }}
      onClick={() => setWizardOpen(false)}
    >
      <div
        className="relative rounded-2xl p-8 w-full max-w-[540px] mx-4"
        style={{ background: 'var(--color-card)', boxShadow: 'var(--shadow-card)' }}
        onClick={e => e.stopPropagation()}
      >
        {/* Header */}
        <div className="flex items-center justify-between mb-6">
          <span className="font-semibold text-base" style={{ color: 'var(--color-ink)' }}>
            {t('ai.wizardTitle')}
          </span>
          <button onClick={() => setWizardOpen(false)} style={{ color: 'var(--color-muted)' }}>✕</button>
        </div>

        {/* Dots */}
        {step < 4 && (
          <div className="flex gap-2 mb-8">
            {dots.map(i => (
              <div
                key={i}
                className="w-2 h-2 rounded-full transition-all"
                style={{ background: i === step ? 'var(--color-primary)' : 'var(--color-border)' }}
              />
            ))}
          </div>
        )}

        {/* Step 0: Música */}
        {step === 0 && (
          <div className="space-y-4">
            <p className="text-sm" style={{ color: 'var(--color-ink)' }}>{t('ai.step1')}</p>
            <SongSearch onAnalysis={handleSongSelect} />
            <button
              onClick={() => setStep(1)}
              className="text-xs mt-2"
              style={{ color: 'var(--color-muted)' }}
            >
              {t('ai.step1Skip')} →
            </button>
          </div>
        )}

        {/* Step 1: Estilo */}
        {step === 1 && (
          <div className="space-y-4">
            <p className="text-sm" style={{ color: 'var(--color-ink)' }}>{t('ai.step2')}</p>
            <div className="flex flex-wrap gap-2">
              {STYLES.map(s => (
                <button
                  key={s}
                  onClick={() => setLocalStyle(s)}
                  className={`px-4 py-2 rounded-xl text-sm font-medium transition-all ${localStyle === s ? 'btn-primary' : 'btn-neumorphic'}`}
                  style={localStyle !== s ? { color: 'var(--color-ink)' } : {}}
                >
                  {s}
                </button>
              ))}
            </div>
            <div className="flex justify-between mt-4">
              <button onClick={() => setStep(0)} className="text-xs" style={{ color: 'var(--color-muted)' }}>{t('ai.back')}</button>
              <button onClick={() => setStep(2)} className="btn-primary px-4 py-2 text-sm rounded-xl">{t('ai.next')}</button>
            </div>
          </div>
        )}

        {/* Step 2: BPM */}
        {step === 2 && (
          <div className="space-y-4">
            <p className="text-sm" style={{ color: 'var(--color-ink)' }}>{t('ai.step3')}</p>
            <div className="space-y-3">
              <input
                type="range"
                min={60}
                max={180}
                value={localBpm}
                onChange={e => setLocalBpm(Number(e.target.value))}
                className="w-full accent-[var(--color-primary)]"
              />
              <div className="flex justify-between text-xs font-mono" style={{ color: 'var(--color-muted)' }}>
                <span>60</span>
                <span className="font-bold text-base" style={{ color: 'var(--color-ink)' }}>{localBpm} BPM</span>
                <span>180</span>
              </div>
              {bpmSuggestion && (
                <button
                  onClick={() => setLocalBpm(bpmSuggestion)}
                  className="text-xs px-3 py-1 rounded-full"
                  style={{ background: 'var(--color-card-hi)', color: 'var(--color-primary)' }}
                >
                  {t('ai.step3Suggestion')} {bpmSuggestion} BPM
                </button>
              )}
            </div>
            <div className="flex justify-between mt-4">
              <button onClick={() => setStep(1)} className="text-xs" style={{ color: 'var(--color-muted)' }}>{t('ai.back')}</button>
              <button onClick={() => setStep(3)} className="btn-primary px-4 py-2 text-sm rounded-xl">{t('ai.next')}</button>
            </div>
          </div>
        )}

        {/* Step 3: Feeling */}
        {step === 3 && (
          <div className="space-y-4">
            <p className="text-sm" style={{ color: 'var(--color-ink)' }}>{t('ai.step4')}</p>
            <div className="flex flex-wrap gap-2">
              {FEELINGS.map(f => (
                <button
                  key={f.id}
                  onClick={() => toggleFeeling(f.id)}
                  className={`px-4 py-2 rounded-full text-sm font-medium transition-all border ${localFeeling.includes(f.id) ? 'btn-primary' : 'btn-neumorphic'}`}
                  style={!localFeeling.includes(f.id) ? { color: 'var(--color-muted)' } : {}}
                >
                  {f.label}
                </button>
              ))}
            </div>
            <div className="flex justify-between mt-4">
              <button onClick={() => setStep(2)} className="text-xs" style={{ color: 'var(--color-muted)' }}>{t('ai.back')}</button>
              <button
                onClick={handleGenerate}
                disabled={generating}
                className="btn-primary px-5 py-2 text-sm rounded-xl flex items-center gap-2"
              >
                {generating
                  ? <span className="inline-block w-3 h-3 border border-white border-t-transparent rounded-full animate-spin" />
                  : '✦'}
                {t('ai.generate')}
              </button>
            </div>
          </div>
        )}

        {/* Step 4: Generating / Result shown in AIPanel */}
        {step === 4 && (
          <div className="space-y-4 text-center py-4">
            <p className="text-sm font-semibold" style={{ color: 'var(--color-primary)' }}>✦ {t('ai.active')}</p>
            <p className="text-xs" style={{ color: 'var(--color-muted)' }}>
              {t('ai.suggestion')} — veja o painel lateral
            </p>
            <button onClick={() => setWizardOpen(false)} className="btn-primary px-4 py-2 text-sm rounded-xl">
              OK
            </button>
          </div>
        )}
      </div>
    </div>
  )
}
```

> **Nota:** `SongSearch` espera uma prop `onAnalysis: (song: SongAnalysis) => void`. Verificar a interface atual do componente no passo 2.

- [ ] **Step 2: Verificar a interface do SongSearch**

```bash
grep -n "onAnalysis\|SongAnalysis\|interface.*Props" "/Volumes/SSD Interno/Projetos ClaudeCode/Reharm/src/components/SongSearch.tsx" | head -20
```

Se a interface for diferente, adaptar a prop usada no `handleSongSelect` e no `<SongSearch />` ao contrato real do componente.

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add src/components/AIWizard.tsx
git commit -m "feat: add AIWizard modal (4-step: music, style, BPM, feeling)"
```

---

## Task 8: AIPanel

**Files:**
- Create: `src/components/AIPanel.tsx`

- [ ] **Step 1: Criar src/components/AIPanel.tsx**

```tsx
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { useAI, useAIInternal } from '../contexts/AIContext'
import { previewChord } from '../audio/player'
import { parseNote } from '../core/parser'

export function AIPanel({ onApply }: { onApply: (chords: string[]) => void }) {
  const { t, i18n } = useTranslation()
  const { panelOpen, session, suggestion, status } = useAI()
  const { setPanelOpen, setSuggestionFromGroq, setStatus, fetchBadges } = useAIInternal()
  const { updateSession, acceptSuggestion, dismissAI } = useAI()

  const [intention, setIntention] = useState<string | null>(null)
  const [showIntention, setShowIntention] = useState(false)
  const [loading, setLoading] = useState(false)

  if (!panelOpen) return null

  const handleCallSuggest = async (intent?: string) => {
    setLoading(true)
    setStatus('loading')
    try {
      const res = await fetch('/api/ai-suggest', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ session, intention: intent, lang: i18n.language }),
      })
      const data = await res.json() as { chords?: string[]; explanation?: string }
      setSuggestionFromGroq({ chords: data.chords ?? [], explanation: data.explanation ?? '' })
    } catch {
      setStatus('idle')
    } finally {
      setLoading(false)
    }
  }

  const handleAccept = () => {
    acceptSuggestion((chords) => {
      onApply(chords)
      // fetch badges for the accepted chords
      fetchBadges(chords, session)
    })
  }

  const handleEdit = () => {
    if (!suggestion) return
    onApply(suggestion.chords)
    setPanelOpen(false)
  }

  // Context summary line
  const contextLine = [
    session.song ? `${session.song.title} (${session.song.artist})` : null,
    session.style,
    session.bpm ? `${session.bpm} BPM` : null,
    session.chords.length > 0 ? session.chords.join(' – ') : null,
  ].filter(Boolean).join(' · ')

  return (
    <div
      className="fixed right-0 top-0 h-full z-40 flex flex-col"
      style={{
        width: '360px',
        background: 'var(--color-card)',
        boxShadow: '-4px 0 24px rgba(0,0,0,0.2)',
        borderLeft: '1px solid var(--color-border)',
      }}
    >
      {/* Header */}
      <div className="flex items-center justify-between px-6 py-4 border-b" style={{ borderColor: 'var(--color-border)' }}>
        <span className="flex items-center gap-2 font-semibold text-sm" style={{ color: 'var(--color-primary)' }}>
          <span className="animate-pulse">✦</span>
          {t('ai.active')}
        </span>
        <button onClick={() => setPanelOpen(false)} style={{ color: 'var(--color-muted)' }}>✕</button>
      </div>

      <div className="flex-1 overflow-y-auto px-6 py-5 space-y-5">
        {/* Context summary */}
        {contextLine && (
          <p className="text-xs rounded-xl px-3 py-2" style={{ background: 'var(--color-bg)', color: 'var(--color-muted)' }}>
            {contextLine}
          </p>
        )}

        {/* No suggestion yet: show call-to-action or intention question */}
        {!suggestion && !loading && (
          <div className="space-y-3">
            {!showIntention ? (
              <>
                <p className="text-sm" style={{ color: 'var(--color-ink)' }}>
                  {t('ai.intentionQuestion')}
                </p>
                <div className="space-y-2">
                  {[
                    { key: 'intentionResolve', id: 'resolve' },
                    { key: 'intentionIntensify', id: 'intensify' },
                    { key: 'intentionSurprise', id: 'surprise' },
                  ].map(opt => (
                    <button
                      key={opt.id}
                      onClick={() => handleCallSuggest(t(('ai.' + opt.key) as any))}
                      className="w-full text-left px-4 py-3 rounded-xl text-sm btn-neumorphic"
                      style={{ color: 'var(--color-ink)' }}
                    >
                      {t(('ai.' + opt.key) as any)}
                    </button>
                  ))}
                </div>
              </>
            ) : null}
          </div>
        )}

        {/* Loading */}
        {loading && (
          <div className="flex items-center gap-3 py-4">
            <span className="inline-block w-4 h-4 border-2 border-[var(--color-primary)] border-t-transparent rounded-full animate-spin" />
            <span className="text-sm" style={{ color: 'var(--color-muted)' }}>{t('ai.generating')}</span>
          </div>
        )}

        {/* Suggestion */}
        {suggestion && !loading && (
          <div className="space-y-4">
            <p className="font-mono text-[10px] uppercase tracking-widest" style={{ color: 'var(--color-muted)' }}>
              {t('ai.suggestion')}
            </p>

            {/* Chord chips */}
            <div className="flex flex-wrap gap-2">
              {suggestion.chords.map((chord, i) => (
                <span
                  key={i}
                  className="px-4 py-2 rounded-xl text-sm font-semibold cursor-default"
                  style={{ background: 'var(--color-primary)', color: 'var(--color-bg)' }}
                >
                  {chord}
                </span>
              ))}
            </div>

            {/* Explanation */}
            <p className="text-sm leading-relaxed" style={{ color: 'var(--color-ink)' }}>
              {suggestion.explanation}
            </p>

            {/* Actions */}
            <div className="flex gap-2 pt-2">
              <button onClick={handleAccept} className="btn-primary flex-1 py-2 text-sm rounded-xl">
                ✓ {t('ai.accept')}
              </button>
              <button onClick={handleEdit} className="btn-neumorphic px-4 py-2 text-sm rounded-xl" style={{ color: 'var(--color-ink)' }}>
                ✏ {t('ai.edit')}
              </button>
            </div>
          </div>
        )}
      </div>

      {/* Footer */}
      <div className="px-6 py-4 border-t" style={{ borderColor: 'var(--color-border)' }}>
        <button
          onClick={dismissAI}
          className="text-xs w-full text-center"
          style={{ color: 'var(--color-muted)' }}
        >
          {t('ai.dismiss')}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add src/components/AIPanel.tsx
git commit -m "feat: add AIPanel sliding panel for mid-session context and suggestion"
```

---

## Task 9: ChordBadge + HarmonicField

**Files:**
- Create: `src/components/ChordBadge.tsx`
- Modify: `src/components/HarmonicField.tsx`

- [ ] **Step 1: Criar src/components/ChordBadge.tsx**

```tsx
import { useState } from 'react'
import type { ChordBadgeData } from '../types'

const BADGE_MAP: Record<string, { emoji: string; label: string }> = {
  good: { emoji: '🟢', label: '' },
  ok:   { emoji: '🟡', label: '' },
  bad:  { emoji: '🔴', label: '' },
}

export function ChordBadge({ badge }: { badge: ChordBadgeData }) {
  const [open, setOpen] = useState(false)
  const { emoji } = BADGE_MAP[badge.level] ?? BADGE_MAP.ok

  return (
    <div className="relative inline-flex" onClick={e => { e.stopPropagation(); setOpen(v => !v) }}>
      <span
        className="text-[10px] cursor-pointer select-none"
        title={badge.explanation}
        style={{ lineHeight: 1 }}
      >
        {emoji}
      </span>

      {open && (
        <>
          {/* Backdrop */}
          <div
            className="fixed inset-0 z-40"
            onClick={() => setOpen(false)}
          />
          {/* Popover */}
          <div
            className="absolute bottom-full left-1/2 -translate-x-1/2 mb-2 z-50 rounded-xl px-3 py-2 text-xs w-52 leading-relaxed"
            style={{
              background: 'var(--color-card)',
              boxShadow: 'var(--shadow-card)',
              border: '1px solid var(--color-border)',
              color: 'var(--color-ink)',
            }}
            onClick={e => e.stopPropagation()}
          >
            <span className="font-semibold">{badge.chord}</span>
            <br />
            {badge.explanation}
          </div>
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Adicionar prop `badges` ao HarmonicField**

Na interface `Props` do `HarmonicField.tsx`, adicionar:
```ts
  badges?: Record<string, ChordBadgeData>
```

Atualizar a assinatura da função:
```ts
export function HarmonicField({ ext, onExtChange, onChordClick, tonicOverride, moodOverride, badges = {} }: Props) {
```

Adicionar o import:
```ts
import { ChordBadge } from './ChordBadge'
import type { ChordBadgeData } from '../types'
```

- [ ] **Step 3: Renderizar badge sobre cada acorde no HarmonicField**

Localizar o botão de acorde no loop `chords.map(chord => ...)` e adicionar o badge acima do nome:

```tsx
                <button
                  key={chord.id}
                  onClick={() => handleChordClick(chord)}
                  onMouseEnter={e => { ... }}
                  onMouseLeave={e => { ... }}
                  className="btn-neumorphic flex flex-col items-center px-4 py-3 rounded-xl min-w-[72px] transition-all relative"
                  style={{ color: 'var(--color-ink)' }}
                >
                  {/* Badge (só aparece quando IA está ativa) */}
                  {badges[chord.name] && (
                    <span className="absolute top-1 right-1">
                      <ChordBadge badge={badges[chord.name]} />
                    </span>
                  )}
                  <span className="font-semibold text-sm">{chord.name}</span>
                  <span className="font-mono text-[10px] mt-0.5" style={{ color: meta.color }}>
                    {chord.roman}
                  </span>
                </button>
```

- [ ] **Step 4: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add src/components/ChordBadge.tsx src/components/HarmonicField.tsx
git commit -m "feat: add ChordBadge and wire badges into HarmonicField"
```

---

## Task 10: App.tsx — integrar tudo

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/main.tsx`

- [ ] **Step 1: Adicionar AIProvider ao main.tsx**

No `src/main.tsx`, envolver o `<App />` com `<AIProvider>`:

```tsx
import { AIProvider } from './contexts/AIContext'

// dentro do createRoot(...).render():
<AIProvider>
  <App />
</AIProvider>
```

Se `<App />` já estiver dentro de `<RouterProvider>` ou similar, envolver dentro deste:
```tsx
<AIProvider>
  <RouterProvider router={...} />
</AIProvider>
```

- [ ] **Step 2: Importar AIWizard e AIPanel no App.tsx**

```tsx
import { AIWizard } from './components/AIWizard'
import { AIPanel } from './components/AIPanel'
import { useAI } from './contexts/AIContext'
```

- [ ] **Step 3: Sincronizar o estado do App com a AISession**

Dentro da função `App`, logo após as declarações de estado existentes, adicionar:

```tsx
  const { updateSession } = useAI()

  // Sync App state → AIContext session whenever it changes
  useEffect(() => {
    updateSession({
      style: genreName,
      bpm: bpm,
      chords: reharm.map(c => c.name),
      tonicNum: autoTonic,
    })
  }, [genreName, bpm, reharm, autoTonic])
```

- [ ] **Step 4: Handler para aplicar sugestão da IA na progressão**

Adicionar dentro de `App`:

```tsx
  const handleAIApply = useCallback((suggestedChords: string[]) => {
    setText(suggestedChords.join(' '))
  }, [])
```

- [ ] **Step 5: Renderizar AIWizard e AIPanel no JSX do App**

No return do componente, antes do `</div>` final do layout, adicionar:

```tsx
      <AIWizard />
      <AIPanel onApply={handleAIApply} />
```

- [ ] **Step 6: Passar badges para HarmonicField**

Localizar o `<HarmonicField ...>` existente e adicionar a prop `badges`:

```tsx
import { useAI } from './contexts/AIContext'

// dentro do componente App:
const { badges } = useAI()

// na renderização:
<HarmonicField
  ext={ext}
  onExtChange={...}
  onChordClick={...}
  tonicOverride={autoTonic}
  moodOverride={autoMoods}
  badges={badges}
/>
```

- [ ] **Step 7: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Esperado: sem erros.

- [ ] **Step 8: Commit**

```bash
git add src/App.tsx src/main.tsx
git commit -m "feat: wire AIProvider, AIWizard, AIPanel into App with session sync"
```

---

## Task 11: Verificação Manual (Golden Path)

- [ ] **Iniciar dev server**

```bash
npm run dev
```

Abrir `http://localhost:5173`.

- [ ] **Verificar: Botão no Navbar**

`✦ Chamar IA` aparece entre línguas e tema toggle. Cor muted. Clicar não abre nada ainda — espera modal.

- [ ] **Verificar: Wizard — zero state**

Sem música selecionada, clicar em `✦ Chamar IA` → modal abre com 4 dots. Passo 1 mostra SongSearch. Digitar "Lionel Richie" → autocomplete aparecer → selecionar → avança para passo 2 (estilo). Selecionar "Deep House" → próximo → slider BPM (deve mostrar sugestão baseada na análise) → ajustar → próximo → feeling chips → selecionar "Soulful" → Gerar. Modal mostra loading → IA retorna → step 4 exibe "ver painel lateral". Fechar modal.

- [ ] **Verificar: Painel lateral com sugestão**

Após wizard, painel lateral deve ter aberto automaticamente mostrando acordes sugeridos (ex: `Fmaj9 Am11 Bbmaj9 C9sus4`) + explicação de 2 linhas. Botão "Aceitar" aplica no campo de texto. Botão "Descartar" fecha painel, botão volta a Idle.

- [ ] **Verificar: Mid-session**

Com música e estilo já selecionados, clicar em `✦ Chamar IA` → abre painel lateral diretamente (não modal). Painel mostra contexto (música · estilo · BPM · acordes) + 3 opções de intenção. Clicar "Me surpreenda" → loading → sugestão aparece.

- [ ] **Verificar: Badges no HarmonicField**

Após aceitar sugestão, badges 🟢🟡🔴 devem aparecer sobre os acordes correspondentes no campo harmônico. Clicar num badge abre popover com explicação de 2 linhas. Clicar fora fecha o popover.

- [ ] **Commit final de validação**

```bash
git add -A
git commit -m "chore: AI Coach feature complete — manual golden path verified"
```

---

## Checklist de cobertura da spec

| Seção | Coberta por |
|-------|-------------|
| 2 — Botão Navbar, 4 estados | Task 4 (AIButton) + Task 6 (Navbar) |
| 2 — Observação silenciosa sem calls automáticas | Task 2 (AIContext — sem side effects) |
| 3 — Wizard 4 passos | Task 7 (AIWizard) |
| 3 — Análise Groq em background no passo 1 | Task 3 (ai-suggest) + Task 7 (SongSearch.onAnalysis → step 1) |
| 4 — Painel lateral 360px | Task 8 (AIPanel) |
| 4 — IA abre com contexto lido, sem perguntas de contexto | Task 8 (AIPanel — contextLine + intention options) |
| 5 — Chips de acordes + preview de áudio | Task 8 (AIPanel — chord chips, hover para preview pendente) |
| 5 — Aceitar/Editar/Descartar | Task 8 (AIPanel handlers) + Task 2 (acceptSuggestion, dismissAI) |
| 6 — Badges 🟢🟡🔴 por acorde | Task 9 (ChordBadge + HarmonicField) |
| 6 — Popover de 2 linhas via Groq | Task 9 (ChordBadge popover) + Task 3 (ai-badges) |
| 6 — 1 call por mudança de acorde | Task 2 (fetchBadges exposta via AIInternal) — integração pós-MVP |
| 7 — Remix Preview sem mudanças | Não tocado |
| 8 — O que não muda | Não tocado |

> **Pendência pós-MVP:** Acionar `fetchBadges` quando o usuário muda um único acorde manualmente no `<ChordInput>`. Não bloqueia o MVP — badges existem para acordes aceitos via IA, não para edição manual ainda.
